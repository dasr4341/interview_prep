import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  mark: {
    display: 'none',
  },
  thumb: {
    width: 30,
    height: 30,
    marginTop: 4,
    backgroundColor: 'transparent',
    border: 'none',
  },
}));
export const sliderBarStyles = {
  bar: { backgroundColor: '#3B7AF7' },
};