import { ICellRendererParams } from 'ag-grid-community';

export default function SchedulerCellRender({ data, type } : { data: ICellRendererParams, type: string}) {
  
  return (<div>Show Data</div>);
}
